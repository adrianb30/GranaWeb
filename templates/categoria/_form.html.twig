{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form.nombre) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.descripcion) }}
    </div>
    <div class="form-group mt-2">
        <button class="btn btn-success form-control">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}

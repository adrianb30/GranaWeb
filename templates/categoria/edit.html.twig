{% extends 'admin/base.html.twig' %}

{% block title %}Edit Categoria{% endblock %}

{% block body %}
    <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center ">
            <div class="col-12 col-md-11 col-lg-10 col-xl-9">
                <div class="card bg-dark text-white" style="border-radius: 1rem;">
                    <div class="card-body p-5 text-center">
                        <div class="container">
                            <div class="row ">
                                <h1>Editar Categoria</h1>
                                {{ include('categoria/_form.html.twig', {'button_label': 'Update'}) }}
                                <div class="container">
                                    <div class="row">
                                        <div class="col-6">
                                            <a class="btn btn-outline-secondary " href="{{ path('app_admin_categoria_index') }}">Volver</a>
                                        </div>
                                    <div class="col-6">
                                        {{ include('categoria/_delete_form.html.twig') }}
                                    </div>
                                </div>                        
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'admin/base.html.twig' %}

{% block title %}Categoria index{% endblock %}

{% block body %}
    <h1>Categoria index</h1>
    <div>
        <a class="btn btn-primary m-2" href="{{ path('app_admin_categoria_new') }}">Crear nueva</a>
    </div>
    <table class="table">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorium in categorias %}
            <tr>
                <td>{{ categorium.id }}</td>
                <td>{{ categorium.nombre }}</td>
                <td>{{ categorium.descripcion }}</td>
                <td>
                    <a class="btn btn-outline-info" href="{{ path('app_admin_categoria_show', {'id': categorium.id}) }}">show</a>
                    <a class="btn btn-outline-success" href="{{ path('app_admin_categoria_edit', {'id': categorium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
